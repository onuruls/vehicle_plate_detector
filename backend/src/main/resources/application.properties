# Application configuration
spring.application.name=vehicle_plate_detector
server.port=8080

# Debug configuration (set to true to save debug images)
app.debug.enabled=false
app.debug.dir=${java.io.tmpdir}/vehicle-plate-debug